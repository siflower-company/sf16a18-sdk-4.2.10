#!/bin/sh /etc/rc.common

START=130

USE_PROCD=1
PROG=/etc/autotest/auto_reboot.sh

start_service(){
    procd_open_instance
    procd_set_param command "$PROG" "$1"
    procd_close_instance
}

stop_service(){
	echo "stop reboot" > /dev/ttyS0
}
